const create = document.querySelectorAll(".row")[0].children[0];
const joinbtn = document.querySelectorAll(".row")[0].children[1];

console.log(joinbtn);

joinbtn.addEventListener("click", (event) => {
  console.log(event);
  // joinbtn.remove();
  // create.remove();
})
